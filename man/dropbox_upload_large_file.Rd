% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/upload.R
\name{dropbox_upload_large_file}
\alias{dropbox_upload_large_file}
\title{Upload een groot bestand naar Dropbox via chunked upload session}
\usage{
dropbox_upload_large_file(
  local_path,
  dropbox_path,
  token = dropbox_token(),
  chunk_size = 128 * 1024 * 1024,
  mode = "overwrite"
)
}
\arguments{
\item{local_path}{Volledig lokaal pad van het te uploaden bestand.
Gooit een fout als het bestand niet bestaat.}

\item{dropbox_path}{Doelpad op Dropbox inclusief bestandsnaam,
bijv. \code{"/data/groot_bestand.csv"}. Moet beginnen met \code{/}.}

\item{token}{Dropbox API access token als character string. Standaard
wordt automatisch een token opgehaald via \code{\link[=dropbox_token]{dropbox_token()}}.}

\item{chunk_size}{Grootte van elk upload-chunk in bytes. Standaard 128MB
(\code{128 * 1024 * 1024}). Grotere chunks zijn sneller maar kwetsbaarder
voor netwerkfouten; kleinere chunks zijn robuuster maar langzamer.}

\item{mode}{Schrijfmodus als character string. Opties:
\describe{
\item{\code{"overwrite"}}{Overschrijft een bestaand bestand (standaard).}
\item{\code{"add"}}{Voegt toe; hernoemt automatisch bij naamconflict.}
\item{\code{"update"}}{Vereist een revisie-ID; voor conflictvrije updates.}
}}
}
\value{
Invisibly de Dropbox bestandsmetadata als lijst, met velden zoals
\code{name}, \code{path_display}, \code{size}, en \code{server_modified}.
}
\description{
Uploadt bestanden groter dan 150MB naar Dropbox via de upload session API,
waarbij het bestand in chunks wordt verstuurd. Toont voortgang per chunk.
Voor kleinere bestanden is \code{\link[=dropbox_upload_file]{dropbox_upload_file()}} efficiënter.
}
\details{
De upload verloopt in drie fasen via de Dropbox upload session API:

\enumerate{
\item \strong{Start} (\verb{/2/files/upload_session/start}): eerste chunk wordt
verstuurd en een \code{session_id} wordt ontvangen.
\item \strong{Append} (\verb{/2/files/upload_session/append_v2}): tussenliggende
chunks worden verstuurd met de \code{session_id} en huidige byte-offset.
\item \strong{Finish} (\verb{/2/files/upload_session/finish}): laatste chunk wordt
gecombineerd met de commit-metadata (doelpad, schrijfmodus).
}

De verbinding met het bestand wordt via \code{\link[=on.exit]{on.exit()}} gegarandeerd gesloten,
ook bij een fout halverwege de upload.
}
\section{Foutafhandeling}{

Bij een mislukte API-aanroep in een van de drie fasen gooit de functie
een fout met de fase (start/append/finish) en de API-foutmelding. Een
mislukte upload wordt niet automatisch hervat; start opnieuw bij fout.
}

\section{Authenticatie}{

Het token wordt standaard automatisch opgehaald via \code{\link[=dropbox_token]{dropbox_token()}}.
Zie \code{\link[=dropbox_token]{dropbox_token()}} voor het instellen van de benodigde omgevingsvariabelen.
}

\examples{
\dontrun{
# Groot bestand uploaden met standaard chunk-grootte (128MB)
dropbox_upload_large_file(
  local_path   = "D:/data/groot_bestand.csv",
  dropbox_path = "/data/groot_bestand.csv"
)

# Kleinere chunks voor instabiele verbinding (bijv. 32MB)
dropbox_upload_large_file(
  local_path   = "D:/data/groot_bestand.csv",
  dropbox_path = "/data/groot_bestand.csv",
  chunk_size   = 32 * 1024 * 1024
)
}
}
\seealso{
\code{\link[=dropbox_upload]{dropbox_upload()}} als automatische wrapper die kiest op bestandsgrootte. \cr
\code{\link[=dropbox_upload_file]{dropbox_upload_file()}} voor bestanden kleiner dan 150MB. \cr
\code{\link[=dropbox_upload_folder]{dropbox_upload_folder()}} voor het uploaden van een volledige map.
}
