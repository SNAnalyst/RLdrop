% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/upload.R
\name{dropbox_upload}
\alias{dropbox_upload}
\title{Upload een bestand naar Dropbox, kiest automatisch gewone of chunked upload}
\usage{
dropbox_upload(
  local_path,
  dropbox_path,
  token,
  mode = "overwrite",
  large_file_threshold = 140 * 1024 * 1024
)
}
\arguments{
\item{local_path}{Volledig lokaal pad van het te uploaden bestand.}

\item{dropbox_path}{Doelpad op Dropbox inclusief bestandsnaam.
Moet beginnen met \code{/}.}

\item{token}{Dropbox API access token als character string.}

\item{mode}{Schrijfmodus: \code{"overwrite"} (standaard), \code{"add"}, of \code{"update"}.}

\item{large_file_threshold}{Drempel in bytes waarboven de chunked upload
session wordt gebruikt. Standaard 140MB (\code{140 * 1024 * 1024}).
Iets onder de API-limiet van 150MB om ruimte te laten voor headers.}
}
\value{
Invisibly de Dropbox bestandsmetadata als lijst.
}
\description{
Automatische wrapper rond \code{\link[=dropbox_upload_file]{dropbox_upload_file()}} en
\code{\link[=dropbox_upload_large_file]{dropbox_upload_large_file()}}. Bepaalt op basis van de bestandsgrootte
welke uploadmethode wordt gebruikt. Dit is de aanbevolen functie voor
enkelvoudige uploads.
}
\details{
De keuze tussen uploadmethoden:
\itemize{
\item Bestandsgrootte < \code{large_file_threshold}: \code{\link[=dropbox_upload_file]{dropbox_upload_file()}}
\item Bestandsgrootte >= \code{large_file_threshold}: \code{\link[=dropbox_upload_large_file]{dropbox_upload_large_file()}}
}
}
\section{Authenticatie}{

Vereist de scope \code{files.content.write} in je Dropbox app permissions.
Zie \code{\link[=dropbox_upload_file]{dropbox_upload_file()}} voor instructies over het aanmaken van een token.
}

\examples{
\dontrun{
token <- Sys.getenv("DROPBOX_TOKEN")

# Automatische keuze (aanbevolen)
dropbox_upload("D:/data/bestand.parquet", "/data/bestand.parquet", token)

# Groot bestand — zelfde aanroep, chunked wordt automatisch gebruikt
dropbox_upload("D:/data/groot.csv", "/data/groot.csv", token)

# Lagere drempel instellen (chunked al vanaf 50MB)
dropbox_upload(
  local_path           = "D:/data/bestand.csv",
  dropbox_path         = "/data/bestand.csv",
  token                = token,
  large_file_threshold = 50 * 1024 * 1024
)
}
}
\seealso{
\code{\link[=dropbox_upload_file]{dropbox_upload_file()}} voor kleine bestanden. \cr
\code{\link[=dropbox_upload_large_file]{dropbox_upload_large_file()}} voor grote bestanden. \cr
\code{\link[=dropbox_upload_folder]{dropbox_upload_folder()}} voor het uploaden van een volledige map.
}
