% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list.R
\name{dropbox_list_folder}
\alias{dropbox_list_folder}
\title{Lijst de inhoud van een Dropbox-map op}
\usage{
dropbox_list_folder(
  dropbox_folder,
  token = dropbox_token(),
  recursive = FALSE,
  type = "all"
)
}
\arguments{
\item{dropbox_folder}{Pad naar de map op Dropbox, bijv. \code{"/data/parquet"}.
Moet beginnen met \code{/}. Gebruik \code{""} voor de root van je Dropbox.}

\item{token}{Dropbox API access token als character string. Standaard
wordt automatisch een token opgehaald via \code{\link[=dropbox_token]{dropbox_token()}}.}

\item{recursive}{Logisch. Indien \code{TRUE}, worden submappen ook doorlopen.
Standaard \code{FALSE}.}

\item{type}{Welke items retourneren: \code{"all"} (standaard) voor bestanden
en mappen, \code{"file"} voor alleen bestanden, \code{"folder"} voor alleen mappen.}
}
\value{
Een data.frame met de volgende kolommen:
\describe{
\item{\code{naam}}{Bestandsnaam of mapnaam.}
\item{\code{pad}}{Volledig pad op Dropbox zoals weergegeven (\code{path_display}).}
\item{\code{type}}{\code{"file"} of \code{"folder"}.}
\item{\code{grootte}}{Bestandsgrootte in bytes. \code{NA} voor mappen.}
\item{\code{gewijzigd}}{Wijzigingsdatum (\code{client_modified}) als POSIXct in
UTC. \code{NA} voor mappen.}
}

Het data.frame is gesorteerd op pad.
}
\description{
Haalt de inhoud van een map op Dropbox op en retourneert een overzichtelijk
data.frame met metadata per item (bestanden en optioneel mappen).
Ondersteunt recursief listen en automatische paginering bij grote mappen
(>2000 items).
}
\details{
Gebruikt de Dropbox \code{list_folder} API met automatische paginering.
Bij mappen met meer dan 2000 items worden alle pagina's automatisch
opgehaald via de \code{list_folder/continue} cursor.

De kolom \code{gewijzigd} bevat de \code{client_modified} timestamp — dit is de
datum die de uploadende client heeft meegegeven, niet de server-datum.
Dit komt het meest overeen met de lokale wijzigingsdatum van het bestand.
}
\section{Authenticatie}{

Het token wordt standaard automatisch opgehaald via \code{\link[=dropbox_token]{dropbox_token()}}.
Zie \code{\link[=dropbox_token]{dropbox_token()}} voor het instellen van de benodigde omgevingsvariabelen.
}

\examples{
\dontrun{
# Inhoud van een map bekijken
dropbox_list_folder("/data/parquet")

# Alleen bestanden, recursief
bestanden <- dropbox_list_folder("/projecten/2026", recursive = TRUE, type = "file")

# Alleen directe submappen
submappen <- dropbox_list_folder("/data", type = "folder")

# Totale grootte berekenen
df <- dropbox_list_folder("/data", recursive = TRUE, type = "file")
sprintf("\%.1f MB", sum(df$grootte, na.rm = TRUE) / 1024 / 1024)
}
}
\seealso{
\code{\link[=dropbox_compare_folder]{dropbox_compare_folder()}} om een lokale map te vergelijken met Dropbox. \cr
\code{\link[=dropbox_download_folder]{dropbox_download_folder()}} om een Dropbox-map te downloaden.
}
