% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manage.R
\name{dropbox_move}
\alias{dropbox_move}
\title{Verplaats of hernoem een bestand of map op Dropbox}
\usage{
dropbox_move(
  from_path,
  to_path,
  token = dropbox_token(),
  allow_ownership_transfer = FALSE
)
}
\arguments{
\item{from_path}{Huidig pad op Dropbox, bijv. \code{"/data/oud.csv"}.
Moet beginnen met \code{/}.}

\item{to_path}{Nieuw pad op Dropbox, bijv. \code{"/archief/oud.csv"} of
\code{"/data/nieuw.csv"} (voor hernoemen). Moet beginnen met \code{/}.}

\item{token}{Dropbox API access token als character string. Standaard
wordt automatisch een token opgehaald via \code{\link[=dropbox_token]{dropbox_token()}}.}

\item{allow_ownership_transfer}{Logisch. Indien \code{TRUE}, staat Dropbox toe
dat eigendom van het item wordt overgedragen bij verplaatsing naar een
gedeelde map van een andere gebruiker. Standaard \code{FALSE}.}
}
\value{
Invisibly de metadata van het verplaatste item als lijst.
}
\description{
Verplaatst een bestand of map naar een nieuw pad op Dropbox. Kan ook
gebruikt worden om items te hernoemen door het pad te wijzigen maar de
map hetzelfde te houden.
}
\details{
Gebruikt het \verb{/2/files/move_v2} endpoint. Bij het verplaatsen van een
map worden alle bestanden en submappen mee verplaatst. Eventuele
bovenliggende mappen op het doelpad worden niet automatisch aangemaakt —
gebruik \code{\link[=dropbox_create_folder]{dropbox_create_folder()}} als het doelpad nog niet bestaat.

Gooit een fout als het bronpad niet bestaat, het doelpad al bezet is,
of bij onvoldoende rechten.
}
\section{Authenticatie}{

Het token wordt standaard automatisch opgehaald via \code{\link[=dropbox_token]{dropbox_token()}}.
Zie \code{\link[=dropbox_token]{dropbox_token()}} voor het instellen van de benodigde omgevingsvariabelen.
}

\examples{
\dontrun{
# Bestand verplaatsen
dropbox_move("/data/rapport.xlsx", "/archief/rapport.xlsx")

# Bestand hernoemen (zelfde map, andere naam)
dropbox_move("/data/draft.csv", "/data/final.csv")

# Map verplaatsen (inclusief inhoud)
dropbox_move("/projecten/oud", "/archief/oud")
}
}
\seealso{
\code{\link[=dropbox_delete]{dropbox_delete()}} om een item te verwijderen. \cr
\code{\link[=dropbox_create_folder]{dropbox_create_folder()}} om een doelmap aan te maken.
}
