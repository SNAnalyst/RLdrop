% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manage.R
\name{dropbox_copy}
\alias{dropbox_copy}
\title{Kopieer een bestand of map op Dropbox}
\usage{
dropbox_copy(
  from_path,
  to_path,
  token = dropbox_token(),
  allow_ownership_transfer = FALSE
)
}
\arguments{
\item{from_path}{Bronpad op Dropbox, bijv. \code{"/data/rapport.csv"}.
Moet beginnen met \code{/}.}

\item{to_path}{Doelpad op Dropbox, bijv. \code{"/archief/rapport.csv"}.
Moet beginnen met \code{/}.}

\item{token}{Dropbox API access token als character string. Standaard
wordt automatisch een token opgehaald via \code{\link[=dropbox_token]{dropbox_token()}}.}

\item{allow_ownership_transfer}{Logisch. Indien \code{TRUE}, staat Dropbox toe
dat eigendom van het item wordt overgedragen bij kopiëren naar een
gedeelde map van een andere gebruiker. Standaard \code{FALSE}.}
}
\value{
Invisibly de metadata van het gekopieerde item als lijst.
}
\description{
Kopieert een bestand of map naar een nieuw pad op Dropbox. Het origineel
blijft ongewijzigd op de oorspronkelijke locatie.
}
\details{
Gebruikt het \verb{/2/files/copy_v2} endpoint. Bij het kopiëren van een
map worden alle bestanden en submappen mee gekopieerd. Eventuele
bovenliggende mappen op het doelpad worden niet automatisch aangemaakt —
gebruik \code{\link[=dropbox_create_folder]{dropbox_create_folder()}} als het doelpad nog niet bestaat.

Gooit een fout als het bronpad niet bestaat, het doelpad al bezet is,
of bij onvoldoende rechten.
}
\section{Authenticatie}{

Het token wordt standaard automatisch opgehaald via \code{\link[=dropbox_token]{dropbox_token()}}.
Zie \code{\link[=dropbox_token]{dropbox_token()}} voor het instellen van de benodigde omgevingsvariabelen.
}

\examples{
\dontrun{
# Bestand kopiëren
dropbox_copy("/data/rapport.xlsx", "/archief/rapport.xlsx")

# Map kopiëren (inclusief inhoud)
dropbox_copy("/projecten/template", "/projecten/nieuw_project")
}
}
\seealso{
\code{\link[=dropbox_move]{dropbox_move()}} om een item te verplaatsen in plaats van te kopiëren. \cr
\code{\link[=dropbox_delete]{dropbox_delete()}} om een item te verwijderen.
}
