% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{dropbox_search}
\alias{dropbox_search}
\title{Zoek naar bestanden en mappen op Dropbox}
\usage{
dropbox_search(
  query,
  token = dropbox_token(),
  path = NULL,
  max_results = 100L,
  extensions = NULL
)
}
\arguments{
\item{query}{Zoekterm als character string, bijv. \code{"rapport"} of
\code{"2026 Q3"}. Dropbox zoekt op bestandsnaam en mapnaam.}

\item{token}{Dropbox API access token als character string. Standaard
wordt automatisch een token opgehaald via \code{\link[=dropbox_token]{dropbox_token()}}.}

\item{path}{Optioneel pad om de zoekopdracht te beperken tot een specifieke
map, bijv. \code{"/data"}. Standaard \code{NULL} (doorzoekt hele Dropbox).}

\item{max_results}{Maximum aantal resultaten. Standaard 100. Dropbox
retourneert maximaal 1000 resultaten per pagina; bij meer resultaten
wordt automatisch gepagineerd.}

\item{extensions}{Optioneel character vector van bestandsextensies om op
te filteren, bijv. \code{c("csv", "xlsx")}. Zonder punt. Standaard \code{NULL}
(geen filter).}
}
\value{
Een data.frame met de volgende kolommen:
\describe{
\item{\code{naam}}{Bestandsnaam of mapnaam.}
\item{\code{pad}}{Volledig pad op Dropbox (\code{path_display}).}
\item{\code{type}}{\code{"file"} of \code{"folder"}.}
\item{\code{grootte}}{Bestandsgrootte in bytes. \code{NA} voor mappen.}
\item{\code{gewijzigd}}{Wijzigingsdatum (\code{client_modified}) als POSIXct.
\code{NA} voor mappen.}
}

Bij geen resultaten wordt een leeg data.frame met de juiste kolommen
geretourneerd.
}
\description{
Doorzoekt je Dropbox op bestandsnaam en retourneert een data.frame met de
gevonden items. Ondersteunt optioneel beperken tot een specifieke map en
filteren op bestandsextensies. Bij veel resultaten wordt automatisch
gepagineerd.
}
\details{
Gebruikt het \verb{/2/files/search_v2} endpoint. De Dropbox zoekfunctie
zoekt op woorden in bestandsnamen en is niet hoofdlettergevoelig.
Meerdere woorden in de query worden als AND behandeld.

Bij meer resultaten dan een enkele pagina (max 1000 per pagina) wordt
automatisch doorgebladerd via \verb{/2/files/search/continue_v2} totdat
\code{max_results} is bereikt of er geen resultaten meer zijn.
}
\section{Authenticatie}{

Het token wordt standaard automatisch opgehaald via \code{\link[=dropbox_token]{dropbox_token()}}.
Zie \code{\link[=dropbox_token]{dropbox_token()}} voor het instellen van de benodigde omgevingsvariabelen.
}

\examples{
\dontrun{
# Zoek naar alle bestanden met "rapport" in de naam
dropbox_search("rapport")

# Zoek alleen in een specifieke map
dropbox_search("2026", path = "/projecten")

# Zoek alleen CSV en Excel bestanden
dropbox_search("export", extensions = c("csv", "xlsx"))

# Meer resultaten ophalen
alle_parquet <- dropbox_search("parquet", max_results = 500)
}
}
\seealso{
\code{\link[=dropbox_list_folder]{dropbox_list_folder()}} voor het listen van een specifieke map. \cr
\code{\link[=dropbox_file_info]{dropbox_file_info()}} voor metadata van een enkel bestand.
}
