% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_zip.R
\name{dropbox_download_folder_zip}
\alias{dropbox_download_folder_zip}
\title{Download een grote Dropbox-map per submap als zip}
\usage{
dropbox_download_folder_zip(
  dropbox_folder,
  local_folder,
  token = dropbox_token(),
  keep_zip = FALSE,
  overwrite = "newer"
)
}
\arguments{
\item{dropbox_folder}{Pad naar de rootmap op Dropbox, bijv.
\code{"/Datasets/EODHD/hulp"}. Moet beginnen met \code{/}.}

\item{local_folder}{Lokaal pad van de doelmap. Wordt aangemaakt als deze
nog niet bestaat.}

\item{token}{Dropbox API access token als character string. Standaard
wordt automatisch een token opgehaald via \code{\link[=dropbox_token]{dropbox_token()}}.}

\item{keep_zip}{Logisch. Indien \code{TRUE}, worden de zip-bestanden na uitpakken
bewaard. Standaard \code{FALSE}.}

\item{overwrite}{Bepaalt welke bestanden uitgepakt worden als ze lokaal
al bestaan. Wordt doorgegeven aan \code{\link[=dropbox_download_zip]{dropbox_download_zip()}}.
Zie daar voor opties. Standaard \code{"newer"}.}
}
\value{
Invisibly een character vector van lokale paden van uitgepakte mappen.
}
\description{
Downloadt een grote Dropbox-map door elke directe submap afzonderlijk als
zip te downloaden en lokaal uit te pakken. Omzeilt de Dropbox-limiet van
10.000 bestanden per zip-download. Veel sneller dan per-bestand downloaden
via \code{\link[=dropbox_download_folder]{dropbox_download_folder()}}.
}
\details{
De functie haalt eerst de directe submappen op via de Dropbox API
(niet recursief). Elke submap wordt daarna als zip gedownload via
\code{\link[=dropbox_download_zip]{dropbox_download_zip()}}.

Als een submap zelf ook groter is dan 10.000 bestanden of 20GB, zal die
individuele zip-download mislukken met een API-fout. In dat geval moet je
de functie aanroepen op een dieper niveau, bijv. een specifieke submap.

Bestanden direct in de rootmap (niet in een submap) worden gedownload via
\code{\link[=dropbox_download_zip]{dropbox_download_zip()}} op de rootmap zelf.
}
\section{Authenticatie}{

Het token wordt standaard automatisch opgehaald via \code{\link[=dropbox_token]{dropbox_token()}}.
Zie \code{\link[=dropbox_token]{dropbox_token()}} voor het instellen van de benodigde omgevingsvariabelen.
}

\examples{
\dontrun{
# Grote map per submap downloaden
dropbox_download_folder_zip(
  dropbox_folder = "/Datasets/EODHD/hulp",
  local_folder   = "D:/lokaal/hulp"
)

# Alleen nieuwere bestanden overschrijven
dropbox_download_folder_zip(
  dropbox_folder = "/Datasets/EODHD/hulp",
  local_folder   = "D:/lokaal/hulp",
  overwrite      = "newer"
)
}
}
\seealso{
\code{\link[=dropbox_download_zip]{dropbox_download_zip()}} voor het downloaden van een enkele map als zip.\cr
\code{\link[=dropbox_download_folder]{dropbox_download_folder()}} voor per-bestand downloaden met voortgangsbalk.
}
