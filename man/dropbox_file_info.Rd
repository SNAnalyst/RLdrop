% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata.R
\name{dropbox_file_info}
\alias{dropbox_file_info}
\title{Haal metadata op van een bestand of map op Dropbox}
\usage{
dropbox_file_info(dropbox_path, token = dropbox_token())
}
\arguments{
\item{dropbox_path}{Pad op Dropbox, bijv. \code{"/data/bestand.parquet"} of
\code{"/data"}. Moet beginnen met \code{/}.}

\item{token}{Dropbox API access token als character string. Standaard
wordt automatisch een token opgehaald via \code{\link[=dropbox_token]{dropbox_token()}}.}
}
\value{
Een lijst met de volgende elementen:
\describe{
\item{\code{naam}}{Bestandsnaam of mapnaam.}
\item{\code{pad}}{Volledig pad op Dropbox (\code{path_display}).}
\item{\code{type}}{\code{"file"} of \code{"folder"}.}
\item{\code{grootte}}{Bestandsgrootte in bytes. \code{NA} voor mappen.}
\item{\code{gewijzigd}}{Wijzigingsdatum (\code{client_modified}) als POSIXct.
\code{NA} voor mappen.}
\item{\code{revisie}}{Dropbox revisie-ID als character string. \code{NA} voor mappen.
Bruikbaar voor \code{mode = "update"} bij uploads.}
}
}
\description{
Vraagt de metadata op van een enkel bestand of map op Dropbox, zonder de
inhoud te downloaden. Retourneert een lijst met naam, pad, type, grootte
en wijzigingsdatum.
}
\details{
Gebruikt het \verb{/2/files/get_metadata} endpoint. Gooit een fout als het pad
niet bestaat. Gebruik \code{\link[=dropbox_exists]{dropbox_exists()}} om eerst te controleren of het
pad bestaat.
}
\section{Authenticatie}{

Het token wordt standaard automatisch opgehaald via \code{\link[=dropbox_token]{dropbox_token()}}.
Zie \code{\link[=dropbox_token]{dropbox_token()}} voor het instellen van de benodigde omgevingsvariabelen.
}

\examples{
\dontrun{
# Metadata van een bestand
info <- dropbox_file_info("/data/bestand.parquet")
sprintf("Grootte: \%.1f MB", info$grootte / 1024 / 1024)

# Metadata van een map
info <- dropbox_file_info("/projecten/2026")
info$type  # "folder"
}
}
\seealso{
\code{\link[=dropbox_exists]{dropbox_exists()}} om te controleren of een pad bestaat. \cr
\code{\link[=dropbox_list_folder]{dropbox_list_folder()}} voor metadata van alle items in een map.
}
