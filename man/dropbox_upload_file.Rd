% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/upload.R
\name{dropbox_upload_file}
\alias{dropbox_upload_file}
\title{Upload een bestand naar Dropbox direct via API}
\usage{
dropbox_upload_file(local_path, dropbox_path, token, mode = "overwrite")
}
\arguments{
\item{local_path}{Volledig lokaal pad van het te uploaden bestand,
bijv. \code{"D:/data/bestand.parquet"}. Gooit een fout als het bestand
niet bestaat.}

\item{dropbox_path}{Doelpad op Dropbox inclusief bestandsnaam,
bijv. \code{"/data/bestand.parquet"}. Moet beginnen met \code{/}.}

\item{token}{Dropbox API access token als character string. Genereer via
\url{https://www.dropbox.com/developers/apps} onder het tabblad
\code{Settings} van je app.}

\item{mode}{Schrijfmodus als character string. Opties:
\describe{
\item{\code{"overwrite"}}{Overschrijft een bestaand bestand (standaard).}
\item{\code{"add"}}{Voegt toe; hernoemt automatisch bij naamconflict.}
\item{\code{"update"}}{Vereist een revisie-ID; voor conflictvrije updates.}
}}
}
\value{
Invisibly de Dropbox bestandsmetadata als lijst, met velden zoals
\code{name}, \code{path_display}, \code{size}, en \code{server_modified}.
}
\description{
Uploadt één bestand rechtstreeks via de Dropbox API v2. Geschikt voor
bestanden kleiner dan 150MB. Voor grotere bestanden, gebruik
\code{\link[=dropbox_upload_large_file]{dropbox_upload_large_file()}} of de automatische wrapper \code{\link[=dropbox_upload]{dropbox_upload()}}.
}
\details{
Gebruikt het \verb{/2/files/upload} endpoint van de Dropbox Content API.
De Dropbox API heeft een maximale bestandsgrootte van 150MB voor dit
endpoint. Gebruik \code{\link[=dropbox_upload_large_file]{dropbox_upload_large_file()}} voor grotere bestanden,
of \code{\link[=dropbox_upload]{dropbox_upload()}} als automatische wrapper die de keuze maakt op
basis van bestandsgrootte.
}
\section{Authenticatie}{

Vereist de scope \code{files.content.write} in je Dropbox app permissions.
Maak een token aan via \url{https://www.dropbox.com/developers/apps}.
Sla het token op als omgevingsvariabele:
\code{Sys.setenv(DROPBOX_TOKEN = "jouw_token")}.
}

\examples{
\dontrun{
token <- Sys.getenv("DROPBOX_TOKEN")

# Bestand uploaden met overschrijven (standaard)
dropbox_upload_file(
  local_path   = "D:/data/bestand.parquet",
  dropbox_path = "/data/bestand.parquet",
  token        = token
)

# Metadata opvangen
meta <- dropbox_upload_file("D:/output/result.rds", "/output/result.rds", token)
message("Opgeslagen als: ", meta$path_display)
}
}
\seealso{
\code{\link[=dropbox_upload]{dropbox_upload()}} voor automatische keuze tussen gewone en chunked upload. \cr
\code{\link[=dropbox_upload_large_file]{dropbox_upload_large_file()}} voor bestanden groter dan 150MB. \cr
\code{\link[=dropbox_upload_folder]{dropbox_upload_folder()}} voor het uploaden van een volledige map.
}
