% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{dropbox_compare_folder}
\alias{dropbox_compare_folder}
\title{Vergelijk een lokale map met een Dropbox-map}
\usage{
dropbox_compare_folder(
  local_folder,
  dropbox_folder,
  token = dropbox_token(),
  recursive = FALSE,
  compare_date = TRUE
)
}
\arguments{
\item{local_folder}{Lokaal pad van de map om te vergelijken, bijv.
\code{"D:/data/parquet"}. Gooit een fout als de map niet bestaat.}

\item{dropbox_folder}{Pad naar de map op Dropbox, bijv. \code{"/data/parquet"}.
Moet beginnen met \code{/}. Gebruik \code{""} voor de root van je Dropbox.}

\item{token}{Dropbox API access token als character string. Standaard
wordt automatisch een token opgehaald via \code{\link[=dropbox_token]{dropbox_token()}}.}

\item{recursive}{Logisch. Indien \code{TRUE}, worden submappen ook vergeleken.
Standaard \code{FALSE}.}

\item{compare_date}{Logisch. Indien \code{TRUE} (standaard), wordt ook de
wijzigingsdatum vergeleken. Dropbox \code{client_modified} wordt vergeleken
met de lokale \code{mtime}. Datums worden afgerond op hele seconden.}
}
\value{
Een data.frame met de volgende kolommen:
\describe{
\item{\code{naam}}{Relatief pad van het bestand ten opzichte van de rootmap.}
\item{\code{status}}{Status van het bestand:
\itemize{
\item \code{"gelijk"} — naam, grootte (en optioneel datum) komen overeen.
\item \code{"grootte_verschilt"} — bestand bestaat aan beide kanten maar
verschilt in grootte.
\item \code{"datum_verschilt"} — grootte is gelijk maar datum verschilt
(alleen als \code{compare_date = TRUE}).
\item \code{"alleen_lokaal"} — bestand bestaat alleen in de lokale map.
\item \code{"alleen_dropbox"} — bestand bestaat alleen op Dropbox.
}
}
\item{\code{lokaal_grootte}}{Bestandsgrootte lokaal in bytes, of \code{NA}.}
\item{\code{dropbox_grootte}}{Bestandsgrootte op Dropbox in bytes, of \code{NA}.}
\item{\code{lokaal_datum}}{Wijzigingsdatum lokaal als POSIXct, of \code{NA}.}
\item{\code{dropbox_datum}}{Wijzigingsdatum op Dropbox (\code{client_modified})
als POSIXct, of \code{NA}.}
}

Het data.frame is gesorteerd op status (problemen eerst) en daarna op naam.
}
\description{
Vergelijkt de inhoud van een lokale map met een map op Dropbox op basis van
bestandsnaam, grootte (bytes) en optioneel wijzigingsdatum. Geeft een
overzichtelijk data.frame terug met de status per bestand.

Handig om te controleren of een lokale kopie nog synchroon loopt met
Dropbox, of om te zien welke bestanden ontbreken aan een van beide kanten.
}
\details{
De vergelijking werkt als volgt:
\enumerate{
\item Lokale bestanden worden geinventariseerd via \code{\link[=list.files]{list.files()}} en
\code{\link[=file.info]{file.info()}}. Dropbox-bestanden via de \code{list_folder} API (met
automatische paginering bij >2000 items).
\item Bestandsnamen worden case-insensitive vergeleken (via \code{\link[=tolower]{tolower()}})
omdat Dropbox case-insensitief is.
\item Voor bestanden die aan beide kanten bestaan wordt eerst de grootte
vergeleken. Als \code{compare_date = TRUE} en de grootte gelijk is, wordt
ook de datum vergeleken (met een tolerantie van 2 seconden).
}

Aan het eind wordt een samenvatting naar de console geprint met het
aantal bestanden per status.
}
\section{Authenticatie}{

Het token wordt standaard automatisch opgehaald via \code{\link[=dropbox_token]{dropbox_token()}}.
Zie \code{\link[=dropbox_token]{dropbox_token()}} voor het instellen van de benodigde omgevingsvariabelen.
}

\examples{
\dontrun{
# Snelle vergelijking (platte map, zonder datum)
dropbox_compare_folder(
  local_folder   = "D:/data/parquet",
  dropbox_folder = "/data/parquet",
  compare_date   = FALSE
)

# Diepte-vergelijking met submappen en datumcontrole
resultaat <- dropbox_compare_folder(
  local_folder   = "D:/projecten/2026",
  dropbox_folder = "/projecten/2026",
  recursive      = TRUE,
  compare_date   = TRUE
)

# Alleen bestanden met problemen bekijken
problemen <- resultaat[resultaat$status != "gelijk", ]

# Alleen bestanden die lokaal ontbreken
ontbrekend <- resultaat[resultaat$status == "alleen_dropbox", ]
}
}
\seealso{
\code{\link[=dropbox_download_folder]{dropbox_download_folder()}} om ontbrekende bestanden te downloaden. \cr
\code{\link[=dropbox_upload_folder]{dropbox_upload_folder()}} om ontbrekende bestanden te uploaden.
}
